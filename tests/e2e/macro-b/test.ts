// Macro-B reuses the generic runtime-diagnostics fixture from the A10 harness —
// it is not tied to A10's frozen manifest, only to Playwright's TestInfo/page APIs.
export { test } from '../a10/test';
