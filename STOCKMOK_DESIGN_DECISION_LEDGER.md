# Stockmok Design Decision Ledger

| ID | Gate | Decision | Rationale | Approved by | Status | Affects | Frozen | Superseded |
|---|---|---|---|---|---|---|---|---|
| DEC-001 | 0 | No architecture contradiction found; ARCHITECTURE_CHANGE_REQUIRED = NO | Control pack v4, UI-final 18–35 and audit 36–54 are internally consistent on scope, roles, IA, stock model and implementation constraints | Claude Design | Closed | Whole programme | Yes | — |
| DEC-002 | 1 | Stockmok mark = S1 crossed Stackline, tuned for 16–24 px | Current Stackline-S has a single non-crossing left spine and reads as F/E (ACR-002); S1 fixes the skeleton without spending brand equity | Owner | Closed | Logo, favicon, header, login, public site, monogram sheet | Yes | S2 Shelf S, S3 Monoline S |
| DEC-005 | 1 | Final master geometry: bar 4.4 units, counters 3.2 upper / 3.6 lower, terminal radius 1.2, crossed spine unchanged | Owner asked for instant S recognition at 16–24 px with S2 terminal discipline where it helps legibility; heavier bar and optically unequal counters make the aperture read before the bars | Owner | Closed | All brand assets | Yes | 1a as first drawn |
| DEC-003 | 1 | Keep #1D4ED8, Inter wordmark, 24-unit grid, 4-unit bar, monogram system unchanged | ACR-002 requires refinement, not re-identity; preserves existing token and asset investment | Claude Design | Closed | Brand system | On DEC-002 approval | — |
| DEC-004 | 1 | Design authority for visual expression is Claude Design; deterministic renderer output is reference/QA evidence only | ACR-004 | Owner (pre-approved) | Closed | All visual artifacts | Yes | DETERMINISTIC_UI_RENDER as authority |
| DEC-006-PROPOSAL | 2 | Gate 2 explores all three territories as specified, nine representative screens each | Owner selected full exploration | Owner | Open | Exploration only | No | — |
| DEC-007 | 2 | Territory A expresses density through rules and summary bands rather than KPI cards; 13 px body, 34 px rows | Storekeeper scanning a full 25-row page is the primary operational case | Claude Design | Open (exploration) | Territory A only | No | — |
| DEC-008 | 2 | Territory C marks connection structurally — context band, counterparty identity, dual-unit block, org-attributed timeline — and leaves private surfaces fully neutral | A single badge was the audit's stated failure; the distinction must survive greyscale and small screens | Claude Design | Open (exploration) | Territory C only | No | — |
| DEC-009 | 2 | Deterministic renderer geometry is not treated as frozen; only its token values are reused where they agree with the written design system | Owner steer, section 2 | Owner | Closed | All Gate 2 work | Yes | Pixel-perfect recreation task |
| DEC-010 | 2 | Territory A screens use the final demo state of the canonical chain — Chicken Breast 120 KG, inventory value LKR 691,700.00, low 3, out 1, open POs 0 — on every populated surface | CHG-003; a single state across nine screens keeps every figure cross-checkable against 16 §7.4 | Claude Design | Open (exploration) | Territory A screens | No | — |
| DEC-011 | 2 | The public home page is drawn in the product's own ruled language rather than a separate marketing style, and advertises only built capability | 07 §7 forbids advertising AI, forecasting, marketplace, checkout, POS or a mobile app | Claude Design | Open (exploration) | Territory A only | No | — |
| DEC-012 | 2 | Territory B carries the identical nine subjects and identical canonical figures as Territory A, so the owner compares language and not content | A fair territory comparison requires one variable | Claude Design | Open (exploration) | Territory B screens | No | — |
| DEC-013 | 2 | Each B screen caption names the concrete cost of B's air (px of vertical space, rows lost, scanning speed) rather than praising it | Gate 2 is a decision gate; an honest trade statement is what the owner is choosing between | Claude Design | Open (exploration) | Territory B captions | No | — |
| DEC-006 | 2 | Base visual territory for Gate 3, plus devices carried over from the two unselected territories | Three territories were built to the same nine subjects, the same demo data and the same authority rules, so the choice is between organising ideas, not finishes. Design recommendation: base = Territory C (Connected Operations) — the two-organisation relationship is carried by the layout rather than by labels, and the their-unit-to-your-base-unit arithmetic reads in one pass; amend with Territory A's row density on the product list and movement ledger, and Territory B's first-run and empty states. | Owner | OPEN — blocks Gate 3 |
| DEC-006 | 2 | **APPROVED — BASE = Territory C (Connected Operations).** Inherit from A: compact desktop operational table/ledger density (34–38 px rows) and tabular numeric scanning quality. Inherit from B: onboarding / first-run / empty-state warmth and clearer auth/error/state presentation. | C is the only territory in which the two-organisation relationship is carried structurally rather than by labels; A supplies the density the daily operator needs; B supplies the warmth a first-run owner needs. A and B become reference-only. | Owner | APPROVED — Gate 3 unblocked |
| DEC-007 | 3 | Canonical navigation is the frozen 256 px role-filtered sidebar (LINK-001..016) with the 64 px header; Territory C's ink top bar is not carried over. | File 20 §4.1 freezes sidebar order and role visibility, and file 21 freezes the light-first shell. C's contribution is the relationship grammar, not the shell chrome. | Design (within frozen authority) | Applied in Gate 3 |
| DEC-008 | 3 | Branded login restores Continue with Google and the full role-context contract: requested role is a sign-in preference only, default "Detect automatically", mismatch shows assigned role and offers it. | File 19 FORM-003, file 22 ACTION-003, file 28 COPY-175/304/305. Territory C had dropped both. | Design (restoring frozen authority) | Applied in Gate 3 |
| DEC-009 | 4 | **Gate 4 patches P1–P4 approved.** P1: the danger variant stays, but destructive semantics are limited to Archive, Disable and Cancel order — no generic Delete anywhere in the current release, and the danger IconButton uses an archive box rather than a trash can. P2: COMP-010 StatusPill documents two purchase-order vocabularies — private DRAFT/ORDERED/PARTIALLY_RECEIVED/RECEIVED/CANCELLED and connected DRAFT/SUBMITTED/ACCEPTED/SHIPPED/PARTIALLY_RECEIVED/RECEIVED/REJECTED/CANCELLED — inside one component. P3: 12 px is the minimum for customer-facing product text; 10–11 px is permitted only as annotation on design documents outside the product. P4: Export is not a PageHeader default; it appears only on authorised list/report surfaces and writes only already-fetched bounded rows. | Historical business data is never generically deleted; one component must carry both order vocabularies without becoming a 26th; small type is a documentation affordance, not a product one; universal export invites unbounded reads and unauthorised disclosure. | Owner (pre-authorised) | APPROVED — Gate 4 = OWNER_APPROVED, component count 25/25 | Design system, every future screen | Yes | Gate 4 conditional status |
| DEC-010 | 5 | Gate 5 auth and onboarding contract: one neutral failure sentence ("Email or password is incorrect") across sign-up, global login and branded login; Google popup, popup-blocked and provider-failure states resolve inside the frozen error grammar with no provider codes; password reset is non-enumerating; the workspace handle is immutable, its availability check is advisory only and HANDLE_TAKEN may still occur at submit; invitation acceptance requires the invited address; copy-link exists only in the invitation dialog and is shown once. | Firebase identity plus membership is the only authority — a displayed or requested role never grants permission — and every failure must leave the person a next step without disclosing who has an account or which businesses use Stockmok. | Design (within frozen authority) | APPLIED — GATE_5 = COMPLETE | Gate 5 family; all future auth surfaces | Yes | — |
| DEC-011 | 6 | Gate 6 dashboard information model: exactly five KPI concepts (inventory value, active products, low stock, open purchase orders, awaiting receipt), one Needs Attention list, one approved chart (stock-status counts) with an equal table alternative, and three bounded supporting lists. Inventory value is stated as replacement cost at today's unit costs. No turnover, forecast, valuation history, supplier scorecard, global search or generic export. | The dashboard read budget allows only bounded summary reads, and any figure the data cannot justify would be read as a judgement. Counts, not value share, are what the stock-status chart can honestly carry. | Design (within frozen authority) | APPLIED — GATE_6 = COMPLETE | Dashboard and every inventory surface | Yes | — |
| DEC-012 | 6 | Inventory operations discipline: stock movements are immutable and corrected only by a new entry carrying a required reason; every operation states current → change → resulting balance and whether a minimum is crossed; a transfer writes both halves at once; base unit is locked once stock exists; quantities are never typed over on an edit form; products, categories and warehouses are archived, never deleted, and archive is blocked by physical truth (stock on hand, products in a category, default-warehouse status). | The ledger must remain readable months later, and a balance the user cannot explain is worse than no balance. Archiving preserves history that generic deletion would destroy. | Design (within frozen authority) | APPLIED | Products, categories, warehouses, movements, adjustments | Yes | — |
| DEC-013 | 6 | Canonical demo-state restatement. The figures actually drawn are the authority for Gates 6–9: two active store rooms (Cold Room LKR 398,900.00, Main Store LKR 292,800.00) summing to the unchanged frozen total LKR 691,700.00; Chicken Breast MEAT-001 minimum 20.000 KG; open purchase orders 0 and committed value LKR 0.00 as at 13 Aug 2026. | The frozen total, the twelve products, the SKU families and the connected chain are unchanged; only the per-room split, one minimum and the open-order count are restated, because five Gate 6 boards are drawn as one coherent narrative against them (the 2 August correction that takes stock to 20.000 KG and stops being low; the 13 August dashboard on which nothing is awaiting delivery). Restating once, in writing, is preferable to leaving the same figure with two values in the programme record. | Design (within frozen authority) | APPLIED | Every populated surface in Gates 6–9 | Yes | Checkpoint CANONICAL_FIGURES as first recorded |
| DEC-014 | 7 | Private procurement contract: a private supplier has no account, receives nothing and confirms nothing, so every state is one the buyer sets; states are Draft → Ordered → Partially received → Received, with Cancelled reachable only from Draft or Ordered and refused once any quantity has been received; receiving always states ordered, already received, receive now, store room, balance now, balance after and outstanding, plus the resulting order state, and is the only action that moves stock; buyers are a directory with no sales workflow drawn, hinted at or disabled; a receipt is corrected by a new entry with a reason, never rewritten. | Cancellation is refused for a physical reason rather than a permission — goods are on the shelf — and stating that reason with the three real ways forward is what makes the refusal legible. Naming all seven receiving figures removes arithmetic from the delivery door, where it is most likely to be done wrong. | Design (within frozen authority) | APPLIED — GATE_7 = COMPLETE | Purchase orders, receiving, suppliers, buyers | Yes | — |
| DEC-015 | 8 | Network discovery and projection contract: a business is found only by typing its exact handle and pressing Look up — no browse, no typeahead, no suggestions, no categories, no nearby businesses, no marketplace — and the not-found message is identical whether a handle is unused or simply not shown; a Partner Catalog is a projection of four fields (published name, partner code, unit of sale, optional listed price) and never inventory; exactly nine things cross between two workspaces and the list is closed; a connection is disabled, never deleted, and disabling states what survives before what stops. | A search that answers on every keystroke enumerates who exists; a projection that carries a quantity turns every partner into a stock auditor. Stating the boundary as a closed list of nine makes it testable — a tenth item on any later screen is a defect rather than a judgement call. | Design (within frozen authority) | APPLIED — GATE_8 = COMPLETE | Connections, partner catalog, every connected surface | Yes | — |
| DEC-016 | 9 | Verified mapping contract: five stages — connected supplier, exact partner SKU with an explicit Look up, semantic confirmation in the buyer's own words, conversion, review — creating a mapping whose state is Verified; the supplier is never asked, never told and cannot see it; one partner item maps to exactly one product; the conversion runs one way only, their unit into yours; editing asks for the confirmation again and never re-computes past receipts; eight refusals (no active connection, SKU not found, item not published, buyer product archived, confirmation missing, unusable conversion, connection ended mid-flow, duplicate mapping) are each drawn at the stage that detects them, with the last two re-tested at the moment of creation. | A wrong factor is silent for weeks and then unexplainable, so the flow spends five screens buying one number and a human confirmation. Verified names the person in the buyer's own workspace who confirmed it, which is why no supplier approval exists. | Design (within frozen authority) | APPLIED — GATE_9 = COMPLETE | Product mappings, connected receiving | Yes | — |
| DEC-017 | 9 | Connected order state ownership: Draft → Submitted → Accepted → Shipped → Partially received → Received, with Rejected reachable only from Submitted and Cancelled only from Draft or from Submitted before acceptance; the buyer submits, cancels and receives, the supplier accepts, rejects and ships, and neither side can set the other's state or touch the other's stock; connected orders share the private Purchase Orders list and its filters, distinguished by the CPO prefix and the partner name; receiving is counted in the partner's unit and converted in front of the person recording it, and the partner is told the received quantity in their own unit and nothing else. | Two extra states exist because a real business must agree before anything can be expected. Showing the conversion as a sentence under the field that caused it, and naming the single fact sent to the partner before the button is pressed, keeps both the arithmetic and the disclosure visible at the moment they happen. | Design (within frozen authority) | APPLIED — GATE_9 = COMPLETE | Connected purchase orders, connected receiving, notifications | Yes | — |
| DEC-018 | 9 patch | **Authorised-capability rule.** A UI action may only exist where the frozen authority names a capability behind it. Verified in this run: Print / PDF / document export on an order — NOT authorised, removed (the only authorised export is the bounded CSV on the two report surfaces). Re-enabling a disabled connection — NOT authorised (the connection commands are request, response and disable), removed and replaced with a plain statement that Stockmok does not re-enable one. Restoring a disabled mapping — NOT authorised (the mapping commands are create and disable), removed and replaced with Create a verified mapping. Restoring an archived product — IS authorised, retained wherever it appears. | A button implies a capability, and a design that invents one becomes a specification the build cannot meet. Checking each action against the command list turns three plausible affordances into three deliberate absences, each of which now carries the sentence explaining what the system genuinely does instead. | Design (restoring frozen authority) | APPLIED — GATE_9_PATCH = PASS | Every action on every surface, Gates 5–12 | Yes | Print, Ask to reconnect, Restore the mapping |
| DEC-019 | 10 | **Supporting operations contract.** A notification is created by exactly three things — a product crossing its minimum when the derived status actually changed, a connected order transition, a connection change — is never emailed, carries no figure a partner may not see, and the only field a reader may change is read. Membership is the only grant: one role per person, no role builder, no stacking; the Owner row shows words where destructive buttons would be rather than disabled buttons; removing a member never removes their name from what they recorded. Settings are drawn in three declared kinds — changeable, consequential (each toggle stating what it does NOT change) and permanently fixed (handle, currency, precision, shown greyed and labelled rather than removed). A refusal is a screen, never a silent redirect: wrong role names the required role and the held role, not-found never guesses, and network-off names the setting and who can change it. | Notifications that fire on every issue rather than on a status change train people to ignore them; a settings page that hides an immutable field looks like a missing feature; and a silent redirect converts a permission problem into a rumour that the product is broken. | Design (within frozen authority) | APPLIED — GATE_10 = COMPLETE | Notifications, team, settings, all refusal surfaces | Yes | — |
| DEC-020 | 11 | **Frozen responsive transformations.** Nine element classes — shell and navigation, operational tables, forms, detail layouts, dialogs, connected two-party structures, timelines and ledgers, reports and charts, state screens — each have one declared behaviour at 390, 768, 1280 and 1920. 1280 is the reference width; 1920 adds margin only, with content capped at 1440 px and centred, and never a sixth KPI, a wider table or a larger type scale. A screen inherits its small-screen behaviour from its element classes rather than being reconciled on its own terms. The 44 px rule is a touch rule applied where there is touch, which is why desktop table rows stay at 36 px. | Reconciling forty screens independently produces forty precedents and no contract; freezing the transformation per element class means a screen designed later is already reconciled, and a reviewer checks one table instead of nine precedents. | Design (within frozen authority) | APPLIED — GATE_11 = COMPLETE | Every screen at every width | Yes | — |
| DEC-021 | 12 | **State grammar and accessibility contract, as frames.** Twelve states across nine families, drawn rather than listed: 62 live cells, and 10 cells declared impossible with the reason (a stock operation is one indivisible write, so there is no partial-failure state; an invitation has nothing destructive to confirm). Four bands with one meaning each — red refuses, amber warns and permits, green confirms, grey explains — and no band without a word. Every failure names what is unchanged, in balances the reader can verify; every refusal names the physical obstacle and offers the action that removes it; no message prints an error code, provider name or internal identifier. Skeletons carry geometry only and never a digit. Accessibility is drawn as the thing itself: visible labels above fields, a 4 px focus ring that survives every background, unbroken heading order, status by icon plus word plus reading (proved in greyscale), seven measured contrast pairs all clearing AA, a table that outranks its chart, one trapped dialog at a time, and 44 px close controls. | A state list is unfalsifiable; a state frame can be reviewed. Declaring the ten impossible cells is what stops a later gate filling them with an invented screen, and measuring the seven contrast pairs is what turns "WCAG AA" from a claim into a number. | Design (within frozen authority) | APPLIED — GATE_12 = COMPLETE | Every state on every surface | Yes | — |
| DEC-022 | 13 pre-patch | **Final pre-Gate-13 patch, P1–P6.** Six residual leftovers corrected by restoring frozen decisions rather than making new ones: the connected cancellation refusal restated on the one-shipment model; every reconnect affordance and wording removed; the print sentence removed; the visible invitation route removed while keeping recipient, role, expiry, once-only warning and Copy link; the 390 px bottom-tab navigation removed and the frozen Gate 4 drawer shell restored at both 768 and 390; the 390 px report export removed. | Each leftover was a sentence or control that implied a capability the frozen authority does not name, or a second navigation contract the frozen shell does not have. Correcting copy and removing controls is cheaper and truer than redesigning a screen that is otherwise right. | Design (restoring frozen authority) | APPLIED — FINAL_PRE_G13_PATCH = PASS | Gates 7, 8, 9, 10, 11 and one Gate 2 reference board | Yes | Print, reconnect, bottom tabs, mobile export, raw invite route |
| DEC-023 | 13 | **Gate 13 final reconciliation = PASS.** The complete package reconciles: 40 of 41 registry screens designed and the 41st (Release C storefront) deliberately out of scope; derived states drawn as 62 live cells with 10 declared impossible; nine element classes reconciled at 390/768/1280/1920 with the shell frozen to Gate 4; canonical figures cross-checking on every populated surface; the privacy boundary closed at nine items; 25/25 components; no engineering copy, raw routes, legacy names or unsupported capabilities. One further drift was found and fixed while reconciling: count badges and one table header below the 12 px floor. Two exceptions are recorded rather than hidden — monogram initials and icon glyphs below 12 px are marks inside frozen specimens, and 10–11 px annotations exist only on design boards outside product frames. | A reconciliation gate earns its name only by naming what it did not fix as precisely as what it did. Recording the two type exceptions in writing prevents a later gate from either "discovering" them as defects or quietly widening them into product text. | Design (within frozen authority) | APPLIED — GATE_13 = PASS | Whole package | Yes | — |
| DEC-024 | 14 | **Gate 14 is presented, not self-approved.** The final approval package is drawn as ten boards and handed to the owner with two drawn options, Approve or Request changes. STOCKMOK_DESIGN_FREEZE_v1.0 is deliberately left unwritten until the owner explicitly approves. | The freeze is the owner's decision and the last protection the programme has; a design that records its own approval removes it. | Design | PRESENTED — GATE_14 = AWAITING_OWNER_APPROVAL | Programme closure | No | — |
| DEC-025 | 14 | **GATE 14 APPROVED — STOCKMOK_DESIGN_FREEZE_v1.0 APPROVED.** The owner reviewed the ten-board Gate 14 package and approved it without requested changes on 15 Aug 2026. The Stockmok Release A + B-Lite design package is frozen at v1.0: brand, design system, 25/25 components, five layout families, state grammar, responsive transformations, accessibility contract, canonical data anchors, privacy boundary and authorised-capability rule. DESIGN_PHASE = CLOSED and READY_FOR_FRONTEND_IMPLEMENTATION = YES. No design was modified, regenerated or re-audited on approval, and no production code was started. Implementation may not redesign screens; deviations require a new owner-approved change request recorded here. | The freeze is the owner's decision and the programme's last protection. Recording it once, with the date and the exact scope frozen, is what makes any later deviation visible as a change rather than absorbed as an opinion. | Owner | APPROVED — GATE_14 = APPROVED, freeze recorded | Whole programme; all downstream implementation | Yes | GATE_14 = AWAITING_OWNER_APPROVAL |
