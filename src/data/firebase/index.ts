/**
 * Frontend Firebase adapters belong behind this boundary.
 *
 * F0 intentionally performs no Firebase initialization and exposes no query or
 * command surface. The frozen data and security phases remain authoritative.
 */
export {};
